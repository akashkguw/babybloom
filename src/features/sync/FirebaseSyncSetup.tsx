/**
 * FirebaseSyncSetup — DEPRECATED
 *
 * Firebase credentials are now bundled via VITE_ env vars (#162).
 * Users no longer need to enter Firebase config manually.
 * The sync status UI is now inline in PartnerSync.tsx.
 *
 * This file is kept empty to avoid breaking any stale imports.
 */
export default function FirebaseSyncSetup() {
  return null;
}
